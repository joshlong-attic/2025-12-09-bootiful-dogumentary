applications:
  - name: scheduler
    random-route: false
    path: target/scheduler-0.0.1-SNAPSHOT.jar
    env:
      JBP_CONFIG_OPEN_JDK_JRE: '{ jre: { version: 25.+ } }'
    buildpacks:
      - java_buildpack_offline
    memory: 1G
    instances: 1